<% provide(:title, "Schülerliste") %>
<h3>Nach Klasse sortiert</h3>
<button type="button" class="btn btn-outline-primary">

    <li class="dropdown">
      <a href="#" class="dropdown-toggle black" data-toggle="dropdown">
        Sortieren <b class="caret"></b>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to "Nach Vorname", admin_show_students_vn_path %></li>
        <li><%= link_to "Nach Name", admin_show_students_path %></li>
        <li><%= link_to "Nach Registrierungsstatus", admin_show_students_st_path %></li>
      </ul>
    </li>

</button>
<table class="table table-hover">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Vorname</th>
      <th scope="col">Name</th>
      <th scope="col">Klasse</th>
      <th scope="col">Mail</th>
      <th scope="col">Number</th>
      <th scope="col">Password</th>
      <th scope="col">Registered?</th>
    </tr>
  </thead>
  <tbody>
    <% @student.each do |stud| %>
    <tr>
      <td scope="row"><%= stud.Vorname %></td>
      <td scope="row"><%= stud.Name %></td>
      <td scope="row"><%= stud.Klasse %></td>
      <td scope="row"><%= stud.Mail %></td>
      <td scope="row"><%= stud.Number %></td>
      <td scope="row"><%= stud.Code %></td>
      <td scope="row"><%= stud.Registered %></td>
    </tr>
    <% end %>
  </tbody>
</table>
