<h2>Nach Admins suchen</h2><br/>
<table class="table table-hover">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Vorname</th>
      <th scope="col">Name</th>
      <th scope="col">Mail</th>
      <th scope="col">Number</th>
    </tr>
  </thead>
  <tbody>
    <% @admin.each do |stud| %>
    <tr>
      <td scope="row"><%= stud.vorname %></td>
      <td scope="row"><%= stud.name %></td>
      <td scope="row"><%= stud.mail %></td>
      <td scope="row"><%= stud.number %></td>
      <td scope="row">
        <%= form_tag admin_admin_remove_path do %>
          <%= hidden_field_tag 'number', stud.number %>
          <%= submit_tag "Entfernen", class: "btn btn-secondary", data: { disable_with: "Saving" } %>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= form_tag(admin_admin_remove_path, method: :get) do %>
  <%= label_tag :Nachname %>
  <%= text_field_tag :term %>
  <%= submit_tag 'Suchen' %>
<% end %>
