<div class="center jumbotron">
  <h3>Mails</h3>
  <br />
  <div>
    <%= link_to "Login daten versenden", admin_send_login_path, class: "btn btn-primary", data: { toggle: "modal" } %>
    <%= link_to "Mahnungen versenden", admin_send_remind_path, class: "btn btn-primary" %>
    <%= link_to "Anmeldungsliste versenden", admin_send_list_path, class: "btn btn-primary" %>
  </div>

  <br />
  <h3>Nicht angemeldete Schüler</h3>
  <h6>Diese werden das Mahnmail erhalten</h6>
  <table class="table table-hover">
    <thead class="thead-dark">
      <tr>
        <th scope="col"><%= sort_link "Vorname" %></th>
        <th scope="col"><%= sort_link "Name" %></th>
        <th scope="col"><%= sort_link "Klasse" %></th>
        <th scope="col"><%= sort_link "Mail" %></th>
        <th scope="col"><%= sort_link "Registered" %></th>
      </tr>
    </thead>
    <tbody>
      <% @stud.each do |stud| %>
        <tr>
          <td scope="row">
              <%= stud.Vorname %>
          </td>
          <td scope="row">
              <%= stud.Name %>
          </td>
          <td scope="row"><%= stud.Klasse %></td>
          <td scope="row"><%= stud.Mail %></td>
          <td scope="row"><%= stud.Registered %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
